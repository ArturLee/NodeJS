<%- include('../includes/head.ejs') %>
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <main>
        <% if( order.length <=0 ) { %>
        <h1>
            Nothing here
        </h1>

        <% } else { %>
        <ul>
            <% order.forEach(orders => { %>
            <li>
                <h1># <%= orders.id %></h1>
                <ul>
                    <% orders.products.forEach(products => { %>
                        <li><%= products.title%> (<%= products.orderItem.quantity%>) </li>
                    <%})%>
                </ul>
            </li>
            <%});%>
        </ul>
        <%}%>
    </main>
    <%- include('../includes/end.ejs') %>